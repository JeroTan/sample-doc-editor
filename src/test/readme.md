# Tests

Use Vitest for public behavior seams.

- Put unit tests near the code or under `src/test` when shared fixtures are useful.
- Prefer public interfaces over implementation details.
- Run `npm run test:run` for CI-style tests.
